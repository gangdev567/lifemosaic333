<%@ page language="java" contentType="text/html; charset=UTF-8"
    pageEncoding="UTF-8"%>
<%@ taglib prefix="c" uri="jakarta.tags.core" %>

<div class="card">

	<div class="card-header">
		차 사고
	</div>
	<div class="card-body">
	<c:forEach var="etc" items="${etcs}">
		<div class="row">
			<div class="d-none">${etc.post_id }</div>
				<div class="col-5">${etc.title }</div>
			<div class="col row">
				<div class="col"><span class="pe-2">♥️</span>${etc.like_point }</div>
				<div class="col"><span class="pe-2">🗨️</span>${etc.comment_cnt }</div>
			</div>
		</div>
	</c:forEach>
	</div>

</div>