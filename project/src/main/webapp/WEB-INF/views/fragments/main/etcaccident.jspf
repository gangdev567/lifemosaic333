<%@ page language="java" contentType="text/html; charset=UTF-8"
    pageEncoding="UTF-8"%>
<%@ taglib prefix="c" uri="jakarta.tags.core" %>

<div class="card">

	<div class="card-header">
		기타
	</div>
	<div class="card-body">
	<c:forEach var="car" items="${cars}">
		<div class="row">
				<c:url var="postDetails" value="/post/detail" >
					<c:param name="post_id" value="${car.post_id }" />
				</c:url>
				<div class="col-5">
					<a class="nav-link" href="${postDetails }">
						${car.title }
					</a>
				</div>
			<div class="col row">
				<div class="col"><span class="pe-2">♥️</span>${car.like_point }</div>
				<div class="col"><span class="pe-2">🗨️</span>${car.comment_cnt }</div>
			</div>
		</div>
	</c:forEach>
	</div>

</div>