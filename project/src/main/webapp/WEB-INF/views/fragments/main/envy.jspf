<%@ page language="java" contentType="text/html; charset=UTF-8"
    pageEncoding="UTF-8"%>
<%@ taglib prefix="c" uri="jakarta.tags.core" %>

<div class="card">

	<div class="card-header">
		부럽지?
	</div>
	<div class="card-body">
	<c:forEach var="envy" items="${envies}">
		<div class="row">
				<c:url var="postDetails" value="/post/detail" >
					<c:param name="post_id" value="${envy.post_id }" />
				</c:url>
				<div class="col-5">
				<a class="nav-link" href="${postDetails }">
						${envy.title }
					</a>
				</div>
			<div class="col row">
				<div class="col"><span class="pe-2">♥️</span>${envy.like_point }</div>
				<div class="col"><span class="pe-2">🗨️</span>${envy.comment_cnt }</div>
			</div>
		</div>
	</c:forEach>
	</div>

</div>