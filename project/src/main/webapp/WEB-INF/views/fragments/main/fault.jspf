<%@ page language="java" contentType="text/html; charset=UTF-8"
    pageEncoding="UTF-8"%>
<%@ taglib prefix="c" uri="jakarta.tags.core" %>

<div class="card">

	<div class="card-header">
		내 잘못이야?
	</div>
	<div class="card-body">
	<c:forEach var="falut" items="${faluts}">
		<div class="row">
			<div class="d-none">${falut.post_id}</div>
				<div class="col-5">${falut.title }</div>
			<div class="col row">
				<div class="col"><span class="pe-2">♥️</span>${falut.like_point }</div>
				<div class="col"><span class="pe-2">🗨️</span>${falut.comment_cnt }</div>
			</div>
		</div>
	</c:forEach>
	</div>

</div>